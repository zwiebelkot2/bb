#
# Intentionally empty: API keys / prompt logic / cron are removed for now.
# Add your own env vars later when you re-integrate them.

# Optional: override where editions are cached (useful for external workers)
LNEWS_CACHE_DIR=
